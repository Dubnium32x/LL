#!/usr/bin/env sh
# Route ARM GCC calls to host when running inside Flatpak VS Code.
if [ -n "$FLATPAK_ID" ] && command -v flatpak-spawn >/dev/null 2>&1; then
  exec flatpak-spawn --host arm-none-eabi-gcc "$@"
fi
exec arm-none-eabi-gcc "$@"
